<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Fantasy NHL Guidance - Week {{ week }}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #1e3a8a; color: white; padding: 20px; text-align: center; border-radius: 8px 8px 0 0; }
        .content { background: #f8fafc; padding: 20px; border-radius: 0 0 8px 8px; }
        .summary { background: #e0f2fe; padding: 15px; border-radius: 6px; margin: 15px 0; }
        .recommendation { background: white; padding: 15px; margin: 10px 0; border-left: 4px solid #3b82f6; border-radius: 4px; }
        .insight { background: #fef3c7; padding: 15px; margin: 10px 0; border-radius: 6px; }
        .footer { text-align: center; margin-top: 20px; color: #666; font-size: 12px; }
        h1 { margin: 0; }
        h2 { color: #1e40af; margin-top: 0; }
        ul { margin: 10px 0; padding-left: 20px; }
        li { margin: 5px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏒 Fantasy NHL Guidance</h1>
            <p>Week {{ week }} - {{ team_name }}</p>
            {% if scoring_type %}
            <p><strong>League Type:</strong> {{ scoring_type }}</p>
            {% endif %}
        </div>
        
        <div class="content">
            <div class="summary">
                <h2>📊 Quick Summary</h2>
                <ul>
                {% for bullet in tl_dr %}
                    <li>{{ bullet }}</li>
                {% endfor %}
                </ul>
            </div>
            
            {% if recommendations %}
            <h2>🎯 Recommendations</h2>
            {% for rec in recommendations %}
            <div class="recommendation">
                <strong>Start:</strong> {{ rec.playerIn }}<br>
                <strong>Bench:</strong> {{ rec.playerOut }}<br>
                <strong>Reason:</strong> {{ rec.reason }}
            </div>
            {% endfor %}
            {% endif %}
            
            {% if insights %}
            <h2>💡 Schedule Insights</h2>
            {% for insight in insights %}
            <div class="insight">
                {{ insight.message }}
            </div>
            {% endfor %}
            {% endif %}
            
            <div class="footer">
                <p>Generated by Fantasy Sports Helper</p>
                <p>Source: {{ source_season }}{% if fallback_reason %} ({{ fallback_reason }}){% endif %}</p>
            </div>
        </div>
    </div>
</body>
</html>
